# HuntEval schema contracts v0.3

This directory contains machine-readable compatibility contracts for protocol and persisted artifacts. PR-01 establishes only common primitives. Episode, deployment, trajectory, submission, result, and scoring schemas are added with their owning domain contracts in later pull requests.

Backward-incompatible changes require a new major schema version. Unknown optional fields remain compatible within the same major version.
